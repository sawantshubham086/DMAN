{
  "version": 2,
  "builds": [{ "src": "deadman_bot.py", "use": "@vercel/python" }],
  "routes": [{ "src": "/(.*)", "dest": "deadman_bot.py" }],
  "installCommand": "pip install --no-cache-dir -r requirements.txt"
}
